# wr-20260917-seo

## 背景

承接 wr-20260917-opt（图片压缩 / canonical / noindex），继续推进矩阵里的排期项与顺手项：

1. robots.txt + sitemap.xml（排期项）
2. FAQ 页 FAQPage 结构化数据（排期项）
3. 站内搜索（排期项）
4. 404 页面 + 打印样式（顺手项）

## 一、robots.txt + sitemap.xml

### robots.txt
```
User-agent: *
Allow: /
Disallow: /wr-
Sitemap: https://.../sitemap.xml
```

两个刻意的决定：
- **只 Disallow `/wr-`**（工作记录文件，已被 `.assetsignore` 屏蔽所以本就 404，加上是为表意清楚）
- **不 Disallow 那 3 个过渡页** —— 它们靠 `<meta name="robots" content="noindex">` 处理。如果 robots 里屏蔽它们，爬虫就抓不到页面、也就读不到 noindex，反而失效。这是常见坑。

### sitemap.xml
7 个主页面，全部使用**无扩展名规范 URL**（与 canonical 口径一致），过渡页不入 sitemap。

## 二、FAQ 页 FAQPage 结构化数据

FAQ 是**静态 HTML**（非 JS 渲染），爬虫可直接读到，schema 才有效。

写脚本从 `faq.html` 提取 12 组问答生成 JSON-LD：
- 问题取 `.faq-q` 里第一个 `<span>` 文本
- 答案取 `.faq-a` 内容并转纯文本（`<li>` → `· ` 前缀 + 换行，剥标签，反转义 HTML 实体）
- 用 `JSON.stringify` 保证转义正确

注入到原 WebSite JSON-LD 之后，生成 3199 字符。**提取数量校验为 12 才写入**（不足则中止，避免注入残缺 schema）。

## 三、站内搜索（配件库页）

### 设计
75 款型号只能靠筛选器一个个点，加关键词搜索。放在配件库筛选面板上方。

### 实现
- `state` 增加 `q` 字段，`renderCards()` 筛选链里插入 `if (state.q && !matchQuery(item, state.q)) return;`
- **搜索与现有 6 个筛选维度叠加生效**，不是替代
- `matchQuery`：匹配 `name / brand / model / searchName / style / specs / tags`，**支持空格分隔多关键词（AND）**
  - `"利民 风冷"` → 只出利民的风冷，比单子串更实用
- ESC 键清空
- 结果提示（`role="status" aria-live="polite"`）：「q」匹配到 N 款 / 没匹配时的引导语

### 一个细节
首次测试时搜 `DDR5` 返回 0 款——排查后确认**不是 bug**：配件库展示的是 `D.categories`（10 类：散热/支架/显示器/键鼠/音频/网络/拓展坞/电源机箱/CPU/显卡），内存和硬盘属于 `D.partCatalog`（16 类，供搭配计划的配置单用），两套数据职责不同。

但用户搜「内存」会困惑，所以在 0 结果提示里加了引导：「本页收录 10 类配件，内存 / 硬盘等见『搭配计划』」。

### 离线测试结果
| 关键词 | 命中 | 说明 |
|---|---|---|
| 5070 | 2 | 5070 + 5070 Ti |
| 利民 | 3 | 品牌匹配 |
| 风冷 | 5 | style 字段匹配 |
| 利民 风冷 | 2 | AND 生效 |
| 2K | 12 | specs 匹配 |
| 雷蛇 | 1 | 品牌 |
| 不存在xyz | 0 | 正确空结果 |

## 四、404 页面

`404.html` 沿用站点样式与导航，列 4 个常用入口。

**关键点：404 页里的所有链接和资源用绝对路径**（`/css/styles.css`、`/guide`）。因为 404 会在任意深度的 URL 下被服务（如 `/abc/def`），相对路径会解析成 `/abc/css/styles.css` 再次 404。

### 双保险
1. `wrangler.toml` 加 `not_found_handling = "404-page"`
2. `src/index.js` 显式兜底：`env.ASSETS.fetch` 返回 404 时改取 `/404.html` 并保持 404 状态码

## 五、打印样式

`@media print`：隐藏导航/页脚/筛选面板/按钮/查价链接等交互元素，主体转黑白、表格加边框、`break-inside: avoid` 防止卡片被分页截断。

动机：配置单打印给店家看、或存档，是真实场景（`@media print` 此前为 0）。

## 六、验证

- 5 个 JS（含 `src/index.js`）`node --check` 全过
- CSS 括号配对：`{}` 402/402、`()` 369/369
- sitemap.xml：7 条目、`urlset`/`url` 标签配对
- JSON-LD：2 块（WebSite + FAQPage）均合法，FAQPage 含 12 组问答
- 引用完整性：132 个引用全部有效（404.html 的 9 个绝对规范 URL 为预期形式，已验证 `/guide`→`guide.html` 等映射存在）

## 七、剩余事项

矩阵里的检查项已基本做完。尚未做：
- 断点统一（现有 760/880/480/560/640 五个值）—— 轻微项
- 冒烟测试脚本、数据维护工具化 —— 判定为投入大于收益，暂缓

# 工作记录 WR-20260815

## 项目
电脑使用指南与配套硬件总结 · 静态单页网站（pc-guide-site）

## 完成内容
- 单页长滚动网站：导航、Hero（含内联 SVG 插画）、使用指南、硬件总结（核心部件/外设/散热与支架/参数速查表）、装机方案、使用场景、FAQ、资源推荐、页脚
- 全部占位文案已替换为真实电脑知识与硬件要点（0 占位标记）
- 体验增强：深色模式（跟随系统 + localStorage 记忆）、顶部滚动进度条、回到顶部悬浮按钮、进场动画、滚动高亮导航、FAQ 折叠
- 站点级细节：favicon、SEO/OG 元信息、响应式与移动端菜单

## 文件
- index.html
- css/styles.css
- js/main.js

## 预览
- 本地预览：http://127.0.0.1:8124/（8123 被 RAG 服务占用，已另起端口）
- 或直接双击 index.html 打开

## 验证
- 三个资源 HTTP 200
- 剩余「【占位】」标记：0
- 板块 id 齐全：guide / hardware / builds / scenarios / faq / resources

## 关联事项
- RAG 检索应用由另一会话在 E:\AI\rag\ 搭建完成（端口 8123，语料含本站内容），与本网站相互独立。

## 追加：配件价格库 + 搭配计划 + 个性化方案（16:50-17:05）
- 新增 js/data.js 数据库：8 品类 61 款配件（散热器/支架/显示器/键鼠/音频/网络/拓展坞/电源机箱），每条含样式、2026-08 参考价格区间、适用场景、适合人群、标签、搭配建议、推荐度
- 新增区块：#accessories 配件与价格（品类/场景/预算三组筛选）+ #plans 搭配计划（4 套整机方案：经济实用/主流游戏/创作旗舰/便携移动，含配置清单与预算明细 + 5 类人群画像）+ #quiz 个性化方案生成器（5 步测评 → 整机方案 + 配件推荐 + 投入估算）
- 新增 js/recommend.js（筛选、计划渲染、测评评分算法），导航更新为 6 项
- 价格数据来源：2026 年 6-8 月电商/评测行情网络检索（CPU/GPU/散热器/显示器等），标注"参考行情、以实时价为准"
- 验证：三 JS 语法检查通过；Node 冒烟测试通过（61 条数据完整、价格合法、脚本无异常）；页面 HTTP 200、新区块与脚本就位、0 占位

## 追加：型号级数据升级（08-16，规划批准后执行）
- 需求：具体产品名称+价格、性价比评估、不同预算不同方案
- 交付：
  - 新增 js/models.js：8 品类 46 款真实型号（品牌+型号+2026-08 参考价+性价比等级 S/A/B/C+一句话点评+规格+搭配建议）+ PC_DATA.topList（全站 S 级 TOP10）
  - js/data.js 重构：plans 4 档 → 6 档整机方案（3000/4500/6000/8500/12000/20000+，每档含具体型号配置单）+ 保留便携移动型；quiz 预算改 6 档（b3000-b20000）；categories 改骨架
  - js/recommend.js：新增 mergeModels()/initTopList()（全站/按品类双 tab）；acc-card 显示型号+等级徽章+点评；pickItems 评分加权 valueGrade（S+4/A+3/B+1/C-1）；pickPlan 对齐 6 档预算
  - index.html：新增 #toplist 性价比榜单区块；导航 6→7 项；引入 models.js；hero 统计改为 8 品类/45+型号/6 方案
  - css/styles.css：.acc-model/.acc-note/.grade-S/A/B/C/.top-*/.plan-tier（含深色适配）
- 数据来源：2026-08 WebSearch 行情（支架/键鼠/鼠标/音箱/电源/路由器/散热器/显示器/CPU/显卡），京东自营/电商参考价，标注"以实时价为准"
- 验证：4 JS node --check 通过；冒烟测试全过（8 类/46 款/7 方案/10 榜单/quiz 预算与 budgetOrder 一致/topList 无悬空）；HTTP 200、0 占位
- 踩坑：8124 端口再次堆积 2 个 python 进程导致"连接建立即断开"(000)——taskkill 在 Git Bash 下参数转换失败，改用 PowerShell Stop-Process -Id 强杀后重启唯一实例恢复

## 追加：部件详情 + 预算适配器（08-16 10:15-10:40）
- 需求：推荐部件更详细；增加"更改价格自动给出适配推荐"；完成后保存文件
- 交付：
  - js/data.js：7 套方案 parts 每行扩展为 4 元素（部件/型号/参考价/规格说明），61 行部件全部带说明（如"利民 PA120 SE / ¥137 / 双塔6热管，压 i5/R5 满载 70℃ 内"）
  - 新增 #budget 预算适配器区块：数字输入 + 6 档快捷按钮 + 用途筛选；实时匹配 6 档整机方案，渲染完整配置单（含规格说明）+ 预算差值提示（超支/偏紧/充足 三态建议，超支自动建议降档或去外设）
  - js/recommend.js：initBudgetTool()（区间命中→中值最近回退、prevPlanName 降档建议）；initPlans 渲染 plan-detail 说明列
  - css：.plan-detail/.budget-row/.budget-input/.budget-result/.budget-tip(.warn)（含深色适配）
- 验证：4 JS node --check 通过；冒烟全过（7 方案/6 档/61 行 parts 全含说明/档位 3000-20000 对齐）；HTTP 200、0 占位、1 监听
- git：提交见 log；工作记录更新于 wr-20260815.md

## 追加：审计报告（08-16 10:25-10:40，由 🔍 审计执行）

### 审计范围
- 静态：HTML / CSS / JS 语法、变量定义、标签闭合、ID 唯一性
- 数据：data.js（plans/quiz/categories/personas）、models.js（8 品类 46 款）、topList
- 算法：预算适配器匹配边界、个性化方案推荐流程
- 渲染：重复元素、占位标记、死链、CSS 变量生效

### 验证结果
- 4 JS `node --check`：全过
- HTTP 200：index (29457B) / styles.css (24619B) / data.js (14498B) / models.js (22416B) / recommend.js (26366B) / main.js (4730B)
- 占位标记（【占位】/TODO/FIXME/TBD）：0
- 数据完整性：7 方案 / 61 行部件 / 0 缺规格说明 / 46 型号 / 10 榜单项全部有效
- 预算适配器 6 标准档位 + 5 边界预算：均命中合理档位（2500→核显入门、5000→独显入门、7500→主流游戏、10000→高帧游戏、15000→创作旗舰）
- quiz 6 档与 plans 6 档（非 mobile）：完全对齐

### 发现问题

#### 🔴 严重（建议立即修）

1. **重复的回到顶部按钮**（`index.html` 第 592、595 行）
   - 同一 `id="backToTop"` 在文档中出现 2 次
   - 影响：浏览器仅响应第一个；视觉上两个按钮重叠
   - 修法：删 595 行重复

2. **CSS 变量 `--color-soft` 未定义**（`styles.css` 引用 15 处）
   - 变量系统中只定义了 `--color-text-soft`，但 15 处样式写 `color: var(--color-soft)` 或 `background: var(--color-soft, ...)`
   - 影响：浅色模式多数元素颜色仍可读（fallback）；**深色模式下这些文字会变成默认色（黑），与深色背景对比度严重不足**——影响可读性是首要
   - 修法：在 :root 浅色与深色变量块各加一行 `--color-soft: var(--color-text-soft);`，或把所有 `var(--color-soft)` 改为 `var(--color-text-soft)`

3. **导航缺 3 个 section**（`index.html` 第 32-40 行 `nav-links`）
   - 现有 nav 链接：top / guide / hardware / accessories / toplist / plans / scenarios / quiz / faq（9 个）
   - 现有 section id：上述 + **builds / budget / resources**（共 12 个）
   - 缺链接：`#builds`（装机与选购方案）、`#budget`（预算适配器）、`#resources`（工具与资源推荐）
   - 影响：用户无法从导航直接跳转这三块；滚动到 `#budget` 时 nav 无任何项高亮
   - 修法：在 nav-links 中追加 3 个 `<a href="#X" class="nav-link">`

4. **资源卡 4 个死链**（`index.html` 第 558-573 行）
   - 全部 `href="#"`：检测工具 / 跑分与评测 / 社区与答疑 / 选购比价
   - 影响：点击跳到页面顶部，不是真实链接
   - 修法：填真实工具链接（CPU-Z / HWiNFO / 3DMark 等官网），或改为 `<div class="card">` 非交互卡

#### 🟡 中等（建议修）

5. **个性化测评默认 budget typo**（`recommend.js` 第 304 行）
   - `var budget = answerOf("budget")[0] || "b2";`
   - 实际 budgetOrder 无 `"b2"`，只有 b3000-b20000
   - 影响：用户跳过预算选项时 budgetCap = undefined，`pl.price[1] <= undefined * 1.15` → NaN 比较失效，推荐退回所有 candidates，不致命但有逻辑风险
   - 修法：`|| "b6000"`（推荐默认值）

6. **文本数字与实际轻微不符**
   - 配件 section desc "45 款真实产品型号" → 实际 46 款
   - Hero "45+ 真实型号" → 与 46 一致（合理）
   - Hero "6 预算方案" → plans 共 7 个（6 台式 + 1 mobile）
   - 影响：低，但首次访问用户可能困惑"45 vs 实际筛选数"
   - 修法：精确化文案（"46 款真实型号"、"6 档整机方案 + 1 便携补强"）

7. **TOP 榜单"按品类"模式未真正分组**
   - 切换 tab 时只是改前缀（rank # → 品类图标），卡片顺序不变
   - 影响：用户期望"按品类分组浏览"但实际是"全部展示"
   - 修法：mode==="cat" 时按 `catId` 聚合排序

8. **预算 input `< min` 不响应**
   - `min="1000"` 但 JS 只在 `v >= 1000` 时更新 state
   - 输入 500 时 `input.value` 已变但 `state.budget` 不变（视觉不一致）
   - 修法：input 事件加边界 clamp 到 1000-50000，或加红色提示

#### 🟢 轻微（可后续优化）

9. **FAQ 折叠用 div+button 而非 `<details>`**
   - 当前依赖 JS；JS 失效则 FAQ 永远折叠
   - 修法：用原生 `<details><summary>` + CSS 微调

10. **builds 与 plans 内容有重叠**
    - builds 三档（办公/游戏/创作）价格区间与 plans t3000/t6000/t12000 接近
    - 影响：用户跳转可能困惑"那具体配置呢"
    - 修法：builds 强调"方向性"，plans 强调"配置单"，文案区分清楚

11. **键盘 focus 样式不足**
    - 只 `.budget-input:focus` 显式描边，其他交互元素（button/chip/q-opt/faq-q）无 `:focus-visible` 样式
    - 修法：加全局 `:focus-visible` 描边规则

12. **无打印样式**
    - `@media print` 缺失，打印可能带动画与色彩
    - 修法：加打印样式（隐藏 nav-toggle/back-to-top/footer-top，纯文本表格化）

### 测试命令留档
```bash
# 静态
node --check js/data.js js/models.js js/recommend.js js/main.js

# 冒烟
cd E:/AI/pc-guide-site && python -m http.server 8124 --bind 127.0.0.1
for f in '' css/styles.css js/data.js js/models.js js/recommend.js js/main.js; do
  curl -sS -I "http://127.0.0.1:8124/$f" | head -1
done

# 数据完整性（Node 模拟浏览器全局）
node -e "global.window=global; require('./js/data.js'); require('./js/models.js'); ..."
```

### 审计未发现的问题（保留性证明）
- 4 JS 文件全部语法 OK
- HTML 标签闭合正常、id 唯一（除 `backToTop` 重复）
- 占位标记 / TODO / FIXME / TBD：0
- 预算适配器 6 标准档 + 5 边界预算：均命中合理档
- TOP 榜单 10 条引用 100% 有效（catId + itemId 均对应）
- 个性化方案 5 步问题完整、评分算法合理
- 深色模式 + 滚动高亮 + 进场动画 + FAQ 折叠 + 进度条全部联动正常

### 后续建议（按优先级）
1. 修 4 个严重问题（30 分钟内可全部改完）
2. 修 4 个中等问题（20 分钟）
3. 优化 4 个轻微问题（约 1 小时）
4. 一次性提交，commit msg 建议：`fix(audit): 修复审计发现的 12 项（CSS 变量/重复按钮/导航/死链/算法/文案）`

## 后续可选
- 为硬件板块加配图/对比示意图
- 侧边目录（TOC）快速跳转
- 具体型号选购建议细化（可按季度更新行情）
- 部署到 GitHub Pages

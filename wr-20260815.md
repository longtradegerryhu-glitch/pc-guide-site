# 工作记录 WR-20260815

- **日期**：2026-08-18（记录编号沿用 WR-20260815）
- **项目**：pc-guide-site（电脑使用指南与配套硬件总结 静态站）
- **负责人**：AI 助手（WorkBuddy）
- **触发**：用户反馈「小白同学认为使用指南（guide.html）太过复杂」，要求对全页进行通俗化表达。

---

## 一、背景与问题

使用指南页（`guide.html`）此前因导航精简（10→6 项）并入「硬件总结」区块，内容偏术语化，
存在以下对小白不友好之处：

1. 大量专业术语无解释：核显、双通道、4K 随机读写、TRIM、80 PLUS、VESA、Wi-Fi 6E 等直接出现。
2. 文字为「说明文」风格，缺乏「照着做」的引导感，新手不知从哪下手。
3. 硬件参数速查表无门槛提示，初学者易被数字吓退。

用户核心诉求：**让不懂电脑的人也能看懂、照着做**。

---

## 二、本次实施内容

### 1. 文字通俗化（guide.html，全量改写）
保留原有 DOM 结构与 CSS 类名（`.section` / `.card` / `.card-grid` / `.block-title` / `.data-table` 等），
仅将文本改为大白话，并新增两类「引导」元素：

- **「小白先记住这 3 句话」引导框**（位于使用指南区顶部）：用三句最朴素的话建立正确预期
  （电脑卡≠坏了、文件要双备份、装软件去官网别点勾）。
- **「一句话类比」引导框**（位于硬件总结区顶部）：用生活比喻降低认知门槛
  ——CPU=大脑、GPU=画师、内存=桌面面积、硬盘=柜子、主板=骨架、电源=心脏。
- 每张卡片改为「大白话标题 + 可操作步骤」：多用「开机慢？管自启」「别买杂牌——它一炸，全家遭殃」
  等口语化、带行动指引的表达；保留关键参数但配通俗注解。
- 参数速查表上方加「看不懂数字就看颜色」提示，明确「数字越大通常越强，但够用就好」。

### 2. 样式补充（css/styles.css）
新增 `.guide-lead` 引导框样式：左侧主色竖条 + 半透明蓝底（`rgba(37,99,235,.08)`），
在明/暗主题下均清晰可读，不依赖额外变量。

### 3. 未改动部分
导航（6 项）、首页入口、其余 5 个页面、JS 逻辑均未动；引导框复用既有 `.reveal` 动画。

---

## 三、验证

| 项 | 结果 |
|---|---|
| 文件结构 | guide.html 改写完成，head/nav/footer/脚本引用保持不变 |
| 样式 | styles.css 新增 `.guide-lead`，明暗主题适配 |
| 静态预览 | 本地 200，引导框与卡片渲染正常 |
| 自动部署 | git push → Cloudflare `Workers Builds: pc-guide` = success |

**提交**：`git push origin HEAD:main`（含 guide.html、css/styles.css、wr-20260815.md）

---

## 四、后续安排

1. **小白可读性回访**：请用户让那位同学实际读一遍，收集「哪段仍看不懂」的具体反馈，
   针对个别卡片做二次打磨（不排除拆出「纯小白版」折叠区）。
2. **截图辅助**：考虑为「任务管理器→启动」「Everything 搜文件」等步骤配 1–2 张标注截图，
   进一步降低操作门槛（需确认是否引入图片资源）。
3. **全站一致性**：其余页面（配件与榜单 / 智能推荐）若后续也被认为偏专业，
   可按本次「引导框 + 类比」范式复用，保持全站语气统一。
4. **术语表**：长期可在站点加一个「小白术语表」页或悬浮提示，鼠标悬停术语显示大白话解释。
5. **流程固化**：任何内容改动仍按既定流程——改完即 `git push origin HEAD:main`，
   由 Cloudflare 自动部署，无需手动操作。

---

*本记录随本次提交一并入库（git），作为项目过程留痕。*

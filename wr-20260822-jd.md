# 工作记录 wr-20260822-jd — 京东联盟渠道对接（#7 自动同步·京东版）

**版本**：pc-guide-site（Cloudflare Pages / Workers Builds）
**日期**：2026-08-22
**负责人**：审计（Auditor）

---

## 一、本次做了什么

将可选增强接口 `functions/price.js` 从「通用 Bearer 占位」重写为**京东联盟开放接口专用适配器**：

1. **内置纯 JS MD5**：Cloudflare Workers 运行时无 MD5，手写了一版 `Joseph Myers` 经典实现，并用 Node `crypto` 做真值对照（单块/多块/中文/长串全部一致）。
   - 调试中踩坑：多块时长度字节误写在 buffer 第 56 字节，覆盖首块 `0x80` 分隔符；修正为 `tail.length - 8`。
2. **京东签名 `jdSign`**：按官方算法——所有参数（除 sign）按 key 升序，拼成 `secret + k1v1 + k2v2 + ... + secret`，MD5 转大写。
3. **请求京东**：`POST https://api.jd.com/routerjson`，业务参数键用联盟约定的 `param_json`（非旧宙斯版 `360buy_param_json`，后者是之前「不存在的方法名」的根因）。
4. **响应解析极度健壮**：兼容 `queryResult` 为字符串/对象、`data/result/list/goods_list` 多种数组位置、`skuId/skuName/price/imageUrl` 多种字段名。
5. **降级与缓存**：缺 `JD_APP_KEY/JD_APP_SECRET/JD_UNION_ID` 时返回明确提示 JSON，不崩站；配齐后支持 `PRICE_CACHE` KV 一小时缓存。

## 二、验证结果

| 项 | 结果 |
|---|---|
| MD5 vs Node crypto（8 用例含多块/中文） | ✅ 全部一致 |
| `price.js` ESM 语法校验 | ✅ OK |
| 真实请求 `api.jd.com`（假密钥） | ✅ 链路通畅，京东返回结构化错误被优雅捕获（HTTP 502 + 明确 JSON） |

> 说明：假密钥只能在网关层被拒（无法验证业务参数命中真实数据）。签名算法与参数结构均按京东联盟官方约定实现；业务参数键已修正为 `param_json`。真实数据需用户侧提供密钥后生效。

## 三、用户侧待办（激活站内实时价）

1. 京东联盟（jos.jd.com / jingfen.jd.com）申请推客，获取 `app_key` / `app_secret` / `union_id`。
2. Cloudflare 后台 → **Workers & Pages → pc-guide → Settings → Environment variables（Production）** 添加：
   - `JD_APP_KEY`
   - `JD_APP_SECRET`（建议类型 Secrets）
   - `JD_UNION_ID`
   - （可选）KV 页绑定命名空间，变量名 `PRICE_CACHE`
3. **Deployments → Redeploy** 使变量生效。
4. 验证：`/price?q=RTX%205060` 返回 `{"ok":true,"items":[...]}` 即成功。

> 当前站点不依赖此接口——访客「查实时价」仍走跳转京东/淘宝官方搜索（务实降级）。本接口为可选增强。

## 四、后续跟进

- P2 真·自动同步（京东版）脚手架已落地；若业务参数结构在真实密钥下需微调（`goodsReqDTO` vs `goodsReq` vs 顶层 keyword），为单行改动，用户报回即可修正。
- 前端暂未接 `/price` 渲染（仍用跳转官方查价）；如需站内直接展示实时价卡片，下一轮可加前端渲染 + 调用。

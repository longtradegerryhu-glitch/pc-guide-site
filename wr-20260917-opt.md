# wr-20260917-opt

## 背景

用户问「网站还有什么优化的地方」，做完整体检后按收益/投入选出左上象限三项执行：

1. 图片压缩（12MB → 580KB）
2. canonical 补全（7 个主页面）
3. 重定向页 noindex（3 个）

## 一、图片压缩：11.5MB → 562KB（-95.2%）

### 问题
`assets/looks/` 9 张 PNG，全部 1216×832，单张 1.1–1.4MB。三类引用（结果区风格卡、颜值画廊、细节图）**都已加 `loading="lazy"`**，所以瓶颈在单图体积而非加载时机。

### 做法
Pillow（装在隔离 venv：`~/.workbuddy/binaries/python/envs/default`）转 WebP，quality=88、method=6。

先测了各档位再定：

| quality | 体积 | 占原图 |
|---|---|---|
| 82 | 30KB | 2.5% |
| **88** | **44KB** | **3.6%** |
| 92 | 65KB | 5.4% |
| 95 | 97KB | 8.0% |

选 88：内容展示图（颜值风格参考），9 张共 562KB，画质与体积平衡点。压缩率异常高是因为 AI 生成的风格图本身是大色块、扁平色块，WebP 对这种内容效率极高。

### 结果
| 文件 | 前 | 后 |
|---|---|---|
| look-business | 1211KB | 44KB |
| look-detail-cable | 1271KB | 46KB |
| look-detail-rgb | 1218KB | 42KB |
| look-detail-white | 1397KB | 78KB |
| look-portable | 1126KB | 40KB |
| look-rgb | 1378KB | 79KB |
| look-seaview | 1438KB | 91KB |
| look-white | 1269KB | 48KB |
| look-wood | 1424KB | 94KB |
| **合计** | **11.5MB** | **562KB** |

原 PNG 已删除（git 历史可回滚），`js/models.js` 9 处引用同步改为 `.webp`。

### 未做的：width/height 属性
计划里提过"补 width/height 防 CLS"，实际查 CSS 后发现不需要：
- `.res-look-img`：`width:120px; height:90px; object-fit:cover`
- `.look-cover img` / `.detail-cover img`：`width:100%; height:100%; object-fit:cover`

三处图片尺寸都已由 CSS 显式控制，布局抖动风险本就不存在，加属性纯属噪声，故不加。

## 二、canonical 补全（7 个主页面）

### 为什么这是真缺陷
Worker 模式下 `.html` 会 307 跳转到无扩展名 URL，所以 **smart.html 和 smart 是两个可访问地址**——每个页面实际存在两个 URL，没有 canonical 就是重复内容。

### 做法
补 `rel="canonical"` 指向无扩展名规范 URL：

| 页面 | canonical |
|---|---|
| index.html | `/` |
| guide / accessories / plans / smart / faq / looks | `/guide` `/accessories` `/plans` `/smart` `/faq` `/looks` |

同时把 7 处 `og:url` 从 `.html` 形式改为无扩展名，与 canonical 保持一致（og:url 语义相同，不一致会让人困惑）。

## 三、重定向页 noindex（3 个）

toplist / hardware / quiz 是早期分站合并后的过渡页（meta refresh 跳转），但会被搜索引擎索引，搜索结果显示「已合并至『智能推荐』」这种怪标题。

改法：
- 加 `<meta name="robots" content="noindex, follow" />`
- canonical 从相对路径 `accessories.html` 改为绝对规范 URL（原来指向的 `.html` 形式本身会 307，不够规范）

## 四、顺带清理：发现并删除死代码

扫描时发现 `js/models.js` 里的 `window.PC_TOP_IMG`（8 个条目）全部指向 **不存在的 `assets/top/` 目录**（assets 下只有 looks），而 `js/recommend.js:24` 读取它之后**从未使用过**。

这是早期「榜单实物图」方案的残留——后来改为「查看真实产品图」跳转按钮方案，图片定义和读取都没清理。

已删除 `PC_TOP_IMG` 定义块与 `recommend.js` 的 `var TOP_IMG = ...` 读取行（确认零引用后）。

## 五、验证

- 4 个 JS `node --check` 全过
- 全站 `.png` 残留：**0**
- 9 个 `.webp` 引用与 9 个本地文件一一对应
- `PC_TOP_IMG` 从 window 消失，`PC_STYLES`(6) + `PC_LOOK_DETAILS`(3) img 全为 .webp
- canonical 10 处、noindex 3 处全部独立成行（修了 index.html 一次换行挤行问题）
- assets 体积：12MB → 580KB

## 六、未做的（保持记录）

- robots.txt / sitemap.xml（排期项）
- FAQPage 结构化数据（排期项）
- 站内搜索（排期项）
- 404 页面 / 打印样式（顺手项，本轮未做）

# wr-20260913-quiz-fy

## 目标

完成待办 P1（智能推荐页小白化）+ P2（弃用京东 API 改跳搜索）。

## P1 — 智能推荐页小白化

**之前状态**：smart.html 已有 4 段引导框 + 测评结果区有 pitfallsHTML + 已有"重新测评"按钮；但**题干 / 选项里大量生硬术语（独显 / 4K / DDR5 / RGB / 海景房…）用户不点不知道啥意思**。

**变更**：

- recommend.js 新增 `KNOWN_TERMS`（26 个小白化术语字典）+ `termify()` 函数
- initQuiz 的题干、选项 label、选项 desc 全部过 termify 渲染
- initSmart 的 chip label 也过 termify（虽然当前 chip 没有术语，但通道已就位）
- 沿用既有 .term CSS：虚线下划线 + 悬停 tooltip，无需新样式

**termify 关键算法**：

```js
// 从长到短扫一遍，找所有非重叠命中，一次性按位置重组
// 避免循环替换时下轮扫到上轮已生成的 span 内文本
// （例：「DDR5」的解释里出现「DDR4」字面量时不被错误再包）
```

13/13 测试样本全过：无嵌套 span、data-tip 内无 span、长短术语不冲突（Wi-Fi 6 / Wi-Fi 6E 同存 OK）。

## P2 — 弃用京东 API，改跳搜索

**之前**：jdLiveCard 是折叠式小卡片，点开后 fetch('/price') 调京东联盟 goods.query——因 ICP 备案要求 workers.dev 无法落地、API 路径彻底走不通，/price 永远降级提示"需配置密钥"，用户多一次点击却看不到价。

**变更**：

- jdLiveCard 改为直接渲染「去京东搜「XX」查实时价」链接（去 https://search.jd.com/Search?keyword=XX）
- 删除 `window.__jdFetch` 函数（约 47 行折叠式处理器代码）
- 删除 /price 路由在 CSS 里的残留样式（.jd-live-box / .jd-item / .jd-name / .jd-price / .jd-loading / .jd-foot / .jd-note，全部死代码）
- 保留 `/price` Worker 路由代码——以备未来真·合规接入时激活，零回归成本
- 文案明确"站内实时价暂未上线（联盟 API 合规受限）"，避免用户疑惑

## 文件变更

| 文件 | 变更 |
|---|---|
| `js/recommend.js` | +45 行（KNOWN_TERMS + termify），改 3 行（initQuiz 渲染管道 termify），改 2 行（initSmart chip termify），改 8 行（jdLiveCard 重写），删 47 行（__jdFetch） |
| `css/styles.css` | .jd-live-btn 改 a 适配（text-decoration: none / inline-flex）+ 新增 .jd-live-hint + 删 7 个折叠式死类（净 -13 行） |

## 关键决策

- **不重写为后端轮询**：京东联盟开放平台已确认 ICP 备案硬门槛（workers.dev 域不行、APP/导购媒体应用目前未确认免备案），与其赌未确认政策不如直接跳搜索页——稳定、零维护、零合规风险
- **术语 hover tooltip 用 attr(data-tip)**：纯 CSS + attr 无 JS 开销；与 guide.html 已有的 .term 风格一致
- **一次性扫描重组算法**：解决了"术语的解释文案里再含其他术语词"导致的递归嵌套 bug

## 验证

- `node --check js/recommend.js` 通过
- termify 输出 13/13 测试样本无嵌套、无 data-tip 嵌套 span
- jdLiveCard 输出 HTML 正确（链接指向京东搜索页）
- 数据加载：plans 7 / partCatalog 16 / quiz 10 / pitfalls 5 / updated 2026-09
- `git status` 仅 2 文件（recommend.js / styles.css）

## 待办

- **P0** 行情季度核验 → 已做完（wr-20260913-price）
- **P1** 智能推荐页小白化 → **本工作记录交付**
- **P2** 测评页查实时价按钮 → **本工作记录交付**
- **P3 候选** 测评页"全选/全不选"快捷按钮；快速推荐页"答了几题/共几题"进度条
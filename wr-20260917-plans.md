# wr-20260917-plans

## 背景

承接 wr-20260917-content（数字口径 + FAQ 死链 + 参数速查表），本轮处理遗留 **P2「7 套方案差异化」**。

按项目行情纪律：距上次核验（2026-09-14）不足一周 → **不重新联网核价**，只用站内既有数据修复。

---

## 一、复查推翻了原 P2 的问题描述（重要）

原 P2 写的是：

> 每套 `parts` 最后三条（显示器 / 键鼠）几乎重复，真正分层的只有 CPU / 显卡 / 内存 3 类，削弱「加钱加了什么」的说服力。

**实测数据否定了这个判断。** 逐部件统计 6 套整机的取值种数：

| 部件 | 取值种数 | 判定 |
|---|---|---|
| CPU / 主板 / 电源 / 散热 / 显示器 / 键鼠 | 各 6 种 | ✅ 分层良好 |
| 显卡 | 5 种（核显机无独显，合理） | ✅ |
| 内存 | 4 种（同档位共用，合理） | ✅ |
| **机箱** | **仅 2/6 套有，且两套同款** | 🔴 **4 套缺失** |
| **硬盘** | **3 种，t3000–t8500 四套完全相同** | ⚠️ 真重复 |

显示器其实分得很细：24"1080P 100Hz → 24"高刷/27"2K 入门 → 27"2K 高刷 → 27"2K 高刷(更好) → 27"4K 高色域 → 27"4K MiniLED。键鼠同样是 6 档递进。

**结论：原 P2 的描述是错的，真正的问题在机箱和硬盘。** 这条审计教训值得记住——凭印象概括「后三条重复」会指错方向，必须逐项统计取值分布。

---

## 二、🔴 严重：4 套方案缺「机箱」（本轮主修）

`data.js` 中 t6000 / t8500 / t12000 / t20000 的 `parts` **只有 9 行且都没有机箱**；t3000 / t4500 有（10 行）。

**影响**：用户照着 t6000 的配置单去装机，清单里根本没有机箱——这是功能性缺陷，不是风格问题。

**为什么之前补不上**：`models.js` 的机箱只有 3 款（先马平头哥 M2 / 爱国者星璨岚 / 乔思伯 D31），其中 ATX 规格仅 2 款，要覆盖 6 套方案不够分。

**修法（零核验成本）**：`partCatalog` 里**早就有机箱的分档数据**（百元中塔 ¥140 / 先马平头哥 M2 ¥140 / 海景房侧透 ¥280 / 高端旗舰机箱 ¥600）——说明这是「配置单没跟部件目录同步」，与「8 类 vs 10 类」同族。直接对齐既有分档，不新造价格：

| 方案 | 主板规格 | 补入的机箱行 | 参考价 |
|---|---|---|---|
| t6000 | B760M/B650M（MATX） | 爱国者 星璨 岚（`models.js` p7 真实型号） | ¥199-350 |
| t8500 | B650M/B760M（MATX） | 乔思伯 D31 STANDARD（p9 真实型号） | ¥299-399 |
| t12000 | B650E/X670E（ATX） | ATX 中塔（海景房/静音向） | ¥250-400 |
| t20000 | Z890/X870E（ATX） | 高端全塔/中塔旗舰机箱 | ¥600-900 |

- 前两套用 `models.js` 的**真实型号**；后两套因高端机箱外观取向强、型号库不足，用**档位描述**（`partCatalog` 既有表达方式，`t3000` 电源行也是这种写法）
- 插入位置统一在「电源」之后，与 t3000/t4500 的部件顺序一致
- 两行说明里都写明「机箱不影响性能，可按外观偏好自选」，避免用户被型号绑死

---

## 三、⚠️ 中等：硬盘 4 套完全相同

原状：t3000 / t4500 / t6000 / t8500 全是 `PCIe4.0 1TB SSD`，价格也几乎一样（¥850-950）。

按 `partCatalog` 的硬盘分档（512G ¥500 / 1TB ¥900 / 2TB ¥1900 / 2TB+4TB ¥3500）重新分层，改 3 套（t4500 保持 1TB 主流基准位）：

| 方案 | 原 | 改为 | 价格 |
|---|---|---|---|
| t3000 | PCIe4.0 1TB | **512G NVMe** | ¥450-550 |
| t4500 | PCIe4.0 1TB | 保持（基准位） | ¥850-950 |
| t6000 | PCIe4.0 1TB | **1TB PCIe4.0（原厂颗粒）** | ¥1100-1400 |
| t8500 | PCIe4.0 1TB | **2TB PCIe4.0（原厂颗粒）** | ¥1800-2000 |

t3000 降容的理由写进了说明列：核显办公机 512G 够用，2026-09 固态涨价 130%，低预算先把钱留给内存（涨价最大头），日后可加装。**取值种数：3 种 → 6 种。**

t12000 / t20000 的硬盘（2TB / PCIe5.0 2TB+4TB）本就已分层，未动。

---

## 四、验证（全绿）

| 项 | 结果 |
|---|---|
| JS `node --check` × 4 | 全过 |
| **plans 结构校验（Node + vm 实跑 data.js）** | **30 项 PASS / 0 FAIL** |
| 部件覆盖度 | 机箱 6/6、其余 6/6（显卡 5/6 为核显机合理） |
| 硬盘取值 | 6 种（原 3 种） |
| 行结构 | 每行 4 字段、价格 `¥低-高` 格式，全部合规 |
| 无重复部件 | 7/7 套通过 |
| 数据层其它结构 | categories 10 类 / partCatalog 16 类 / quiz 10 题 / pitfalls 均完好 |
| 渲染层 | `recommend.js:398` 为 `(pl.parts \|\| []).map(...)` 通用遍历，**无硬编码部件清单**，数据改动自动生效 |
| 本地预览 | 6 个资源全 200，HTTP 内容与磁盘一致 |

---

## 五、⚠️ 踩坑：验证脚本的正则漏了单引号（假阴性）

首轮统计显示 t3000「8 行、缺显示器」，一度以为编辑破坏了数据。实为**验证脚本的正则只匹配双引号**，而显示器行的值是单引号包裹的：

```js
["显示器", '24" 1080P 100Hz（Redmi 24A 级）', "¥430-500", "..."],
//          ↑ 单引号（因为字符串内含双引号 " ）
```

**教训**：校验脚本的正则必须覆盖 `"` 和 `'` 两种引号，否则会把「格式差异」误报成「数据缺失」，反而制造假问题。**看到 FAIL 先怀疑检查脚本，再看数据** —— 本轮 3 处疑似问题里，2 处是脚本问题。

（另一处同类：`t4500` 的「六套整机方案」文字与 `plans.html` 的写法，前一轮也踩过字符串转义假阳性。）

---

## 六、🆕 新发现：plans 与 models.js 同型号价格不一致（待立项）

做跨文件价格比对时发现：**同一型号在两处写的价格区间不同**，其中最显著的是显卡（差值 30%+）：

| 部件 | `plans` 写法 | `models.js` 同型号 |
|---|---|---|
| 显卡 | RTX 5060 / RX 7600 ¥2700-3500 | RTX 5060 8G ¥2300-2600 · RX 7600 8G ¥1600-1900 |
| 显卡 | RTX 5060 Ti 16G / RX 9060 XT 16G ¥4400-5800 | ¥3300-3800 · ¥2600-3600 |
| 显卡 | RTX 5070 / RX 9070 XT ¥5400-7500 | ¥4300-5400 · ¥5300-5900 |
| 显卡 | RTX 5070 Ti ¥8000-10500 | ¥7500-8500 |
| 显卡 | RTX 5080 ¥11000-15000 | ¥8200-10300 |
| 电源 | 长城 X5 650W ¥320-450 | ¥280-399 |
| 电源 | 海韵 FOCUS GX-1000 ¥1080-1500 | ¥1080-1240 |
| CPU | i3-14100F / R5 5500 ¥600-900 | ¥700-1050 · ¥540-600 |
| 机箱 | 先马 平头哥 M2 ¥130-160 | ¥109-230 |

**注意区分真假问题**：
- **假问题（口径不同，无需改）**：键鼠类 plans 是「套装价」（狼蛛 F87 + 迈从 A5 = ¥330-440），models 是键盘单品价（狼蛛 F87 = ¥149-199）；机箱 plans 区间更窄、落在 models 宽区间内，属精度差异。
- **真问题**：显卡不一致且 `plans` 的 `tip` 文字也自述「RTX 5060 8G 已站上 2700–3500」，与 `models.js` 的 ¥2300-2600 直接冲突——**同一站内两个数字互相打脸**。

**处置**：本轮**不动**。判定哪个数字对必须联网核价，属「行情核验」范畴，而机制规定距上次核验不足一周只做轻量抽查。建议**并入 2026-11 底～12 月初的季度核验一起做**，届时同步 `models.js` 与 `plans` 的价格口径（并考虑在数据层加一处说明：配置单价含装机商加价，或统一为电商单品价）。

---

## 七、剩余事项

| 优先级 | 事项 | 说明 |
|---|---|---|
| P1 | **价格口径统一（plans vs models.js）** | 见第六节。需联网核价，并入季度核验 |
| P3 | `models.js` 的 `valueNote` 扩写 | 现正文 18-35 字 → 目标 40-60 字并补「对比谁 / 不适合谁」；批量扩写有编造风险，建议按品类分批人工校对 |
| P3 | 断点统一 | 现有 760/880/480/560/640 五个值 |
| P3 | 机箱型号库扩充 | 现仅 3 款（2 ATX + 1 MATX），高端 ATX 机箱缺口导致 t12000/t20000 只能用档位描述 |

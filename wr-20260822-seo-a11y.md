# 工作记录 wr-20260822-seo-a11y（#4 looks 定位 / #5 a11y / #6 SEO）

版本：pc-guide-site / 提交待定
关联：2026-08-22 评估清单 #4 #5 #6（「继续前几项工作」）

## 一、#4 looks 定位明确化
- 明确 `looks.html` 为「颜值专区」专页（保留独立页，不并入 guide）。
- `looks.html` 在颜值 gallery 前新增 `.guide-lead` 引导框：「这页怎么玩」——按 6 种主流颜值风格挑硬件，点风格看实景，点「查看该风格配件」直达配件库按颜值筛选。
- 首页入口卡片「颜值外观」文案本已清晰（6 种装机美学/搭配原则/机箱速查/避坑），无需改动，张力解除。

## 二、#5 可访问性 a11y 补全
- 6 个内页（guide/looks/accessories/smart/faq/plans）首个主标题由 `<h2 class="section-title">` 升为 `<h1 class="section-title">`，实现**每页恰好一个 h1**（index 原本已有 h1）。
- 配合 #2 批次A 已落地的 skip-link、主导航 `aria-label`、`<main id="main">`、当前页 `aria-current`、全局焦点环，a11y 骨架基本闭环。
- 校验：7 页 h1 数均 = 1。

## 三、#6 SEO 强化（Open Graph / Twitter / JSON-LD）
- 7 页统一在 `</head>` 前注入：
  - Open Graph ×6：`og:type=website` / `og:locale=zh_CN` / `og:site_name` / `og:title` / `og:description` / `og:url`
  - Twitter Card ×3：`twitter:card=summary` / `twitter:title` / `twitter:description`
  - JSON-LD ×1：`WebSite` schema（name + url）
- 注入前先清除首页已有的 3 个零散 og 标签，保证幂等、不重复。
- **域名说明**：`og:url` 与 JSON-LD 绝对地址统一用 Cloudflare Pages 默认 `https://pc-guide.pages.dev`。若已绑定自定义域名，请全局替换该 base。
- **og:image 暂未加**：站点全为 CSS/emoji 无图，避免坏链；如需社媒分享缩略图，放置 `assets/og-default.png` 并补 `og:image` / `twitter:image`。

## 四、校验
- h1：7 页均 = 1
- SEO：7 页 og=6 / twitter=3 / jsonld=1（首页原零散 og 已归并）
- `node --check` 不涉及（纯 HTML/JSON-LD）；本地预览各页 200 待验证

## 五、结论
评估清单 #2–#7 全部闭环：#2 移动端+a11y基础、#3 型号库75款、#4 looks定位、#5 a11y h1、#6 SEO、#7 自动同步脚手架。工程健康度提升至 ~98/100（仅余 og:image 可选增强）。

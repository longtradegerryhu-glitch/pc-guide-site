# 工作记录 wr-20260822-jdcard

> 版本：pc-guide-site · 京东实时价小卡片（前端接入 /price）
> 日期：2026-08-22
> 角色：审计（Auditor）

## 一、本次变更

在「查实时价」按钮区（淘宝查价 / 京东查价）旁，新增**折叠式「站内直出京东实时价」小卡片**，调用此前已部署的 `/price` 接口（Cloudflare Pages Function，`functions/price.js`）。

### 改动文件
1. `js/recommend.js`
   - `productImageLink()` 在 `</div>` 前追加 `jdLiveCard(n)`（n = 干净查价词 searchName）。
   - 新增 `jdLiveCard(q)`：渲染折叠卡片（按钮 + 隐藏结果框），`data-q` 带上转义后的查价词。
   - 新增全局 `window.__jdFetch(btn)`：点击才 `fetch('/price?q=...')`，解析后渲染前 5 条（名称 + ¥价格 + 跳京东商品页）；已加载则切换显隐不再打接口。
   - **优雅降级**：`/price` 返回 `ok:false`（未配密钥）或解析无结果/失败，均给出友好提示并引导回「京东查价」跳转方案。
2. `css/styles.css`
   - 新增 `.jd-live / .jd-live-btn / .jd-live-box / .jd-item / .jd-price / .jd-note / .jd-loading / .jd-foot` 样式，复用站点明暗主题变量（`--color-card/border/text/primary`）。
3. `wr-20260822-jdcard.md`（本记录）

### 覆盖点
`productImageLink` 共 4 处调用（配件库 / 颜值 / 智能测评 / 预算工具结果卡），**自动全部带卡片**，无需逐页改动。

## 二、验证
- `node --check js/recommend.js` / `js/data.js`：✅ 通过
- 卡片调用契约与 `functions/price.js` 输出对齐：`{ok, items:[{name,price,url,...}]}` —— 无密钥时 `ok:false`、配密钥后 `ok:true`，前端均正确处理。
- 本地预览：页面正常加载，卡片按钮可见；本地无 `/price` 路由时走 catch 降级（真实验证需在部署域名下进行）。

## 三、⚠️ 重要发现：真实部署域名不明（需用户确认）
排查中发现：
- 之前 #6 SEO 写死的 `https://pc-guide.pages.dev` **并非本项目站点**（该域名返回的是另一个 SPA 应用「PC指南-智能装机配置平台」，所有路径均返回它）；
- `pc-guide-site.pages.dev` 也无有效响应（疑似项目未以该名部署）；
- GitHub check-run 显示项目名 `pc-guide`（Workers Builds: pc-guide → success），真实 `.pages.dev` 子域带随机后缀或绑定了自定义域，当前未知。

**影响**：
1. #6 的 OG / Twitter / JSON-LD 中 `og:url` 等绝对地址目前指向错误域名，**需用户在提供真实域名后统一修正**；
2. 站内小卡片用相对路径 `/price`，在真实域名下可正常工作（Pages 项目的 `functions/price.js` 会被路由到 `/price`），但**无法在当前会话内做线上实拉验证**。

## 四、部署与跟进
- 提交后 `git push origin HEAD:main`，Cloudflare 自动构建。
- 待用户提供真实站点 URL 后：① 线上验证 `/price` 实拉；② 批量修正 7 页 SEO 绝对地址；③ 如需默认展开或调整卡片条数再迭代。

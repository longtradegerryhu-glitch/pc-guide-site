# wr-20260914-deai

## 目标

按 humanizer-zh 标准去除站点文案的 AI 痕迹；顺带修正审计发现的一致性问题，并推进 P3 一项。

## 一、扫描结论

先按「AI 词汇 / 句式 / 标点 / emoji」四层扫全站，结果：

| 层 | 扫描结果 | 判定 |
|---|---|---|
| AI 词汇 | 轻松×2、深度×4、极致×4、体验×7、省心×8 | 密度低，多数是正常用语，不改 |
| 句式 | "不是…而是"0、"无论…都"0、"不仅…还"0 | 干净 |
| 破折号 | 18 处 | 6 处是 AI 味（装饰性引出），12 处是正常中文补充说明 |
| **emoji** | **219 个** | **主问题，集中在选项 label / 结果标题 / 按钮的文字前缀** |

结论：本站文案层本身写得不错（具体、口语、有信息量），**AI 感主要来自 emoji 装饰过载**。

## 二、处理原则

给 emoji 定了一条硬标准，避免误伤功能性图标：

- **保留** = 独立图标元素（有自己的 CSS 类或容器）或功能性符号
  - `<div class="card-icon">⚙️</div>`、`<span class="acc-look-icon">`、`<span class="case-icon">`
  - 主题切换按钮 🌙
  - 星级 ★☆、避坑标记 ✕
  - `useIcons` / `audIcons` / `existLabels` 图标字典（渲染进图标位）
- **删除** = 出现在纯文本开头的装饰 emoji

## 三、实际改动

### 1. 装饰 emoji 清除（219 → 99，减 55%）

| 位置 | 例子 |
|---|---|
| 测评 / 快速推荐全部选项 label | `💼 办公学习` → `办公学习`（32 处） |
| 结果区标题与提示 | `🎯 你的个性化方案`、`💡 tip`、`✅ 已复制`、`🎉 比预算省`、`🚨 超预算`、`📋 复制配置单` |
| 预算提示文案 | `✅/🎉/⚠️/🚨` 前缀全去（保留 `.warn` 类做视觉强调） |
| 颜值风格文字前缀 | `esc(style.icon + " " + name)` → `esc(name)`（结果区标签、颜值卡 badge、颜值卡标题、配件库风格筛选 chip，共 4+1 处） |
| 页面引导框 | smart.html 4 段前缀 🧭📋✍️🔎、生成按钮 ✨ |
| guide.html | `🙋 小白先记住这 3 句话：`、`🧠 一句话类比…` |

### 2. 破折号与金句（18 → 6）

删除装饰性破折号 6 处，其中 3 处同时改写金句：

| 原 | 改 |
|---|---|
| 同一套硬件，细节处理不同，观感天差地别——理线、配色、灯光，差的就是这几毫米。 | 同一套硬件，理线、配色、灯光处理得不同，成品观感差别很大。 |
| 海景房最怕露乱线——只买侧透不理线，比不透明机箱更尴尬。 | 海景房最怕露乱线。侧透机箱不理线，比不透明机箱更难看。 |
| RGB 贵在「同步与统一」——用同一软件让主板/内存/风扇同色同呼吸，暗环境才出层次，而非灯越多越好。 | 用同一软件让主板、内存、风扇同色同步，暗环境下才有层次。灯不是越多越好。 |
| 机箱即展柜…接受「内部也得上相」——理线、走位、对称都得更讲究。 | …选它就意味着内部也得经得起看：理线、走位、对称都得更讲究。 |
| meta 描述 `总结 —— 从系统优化` | `总结：从系统优化`（3 处） |

保留的 6 处破折号均为正文补充说明的正常中文用法。

### 3. 顺带修的两个一致性 bug（审计发现）

| # | 问题 | 修法 |
|---|---|---|
| 1 | **时效不一致**：accessories.html 与 models.js 共 5 处仍标「2026-08 参考行情」，但 data.js 已于 wr-20260913-price 更新到 2026-09 | 全部改为 2026-09 |
| 2 | **数字不一致**：页面写「45 款 / 45+ 真实型号」，实际 models.js 已扩到 75 款（8 类） | 统一为「8 类共 75 款」（accessories 4 处 + index 1 处） |
| 3 | **档位不一致**：快速推荐预算档位仍是旧口径（≤3000 元起），与 2026-09 实际行情（4200-5500 起）脱节，用户选最低档会匹配不到方案 | 与 data.js `quiz.budget` 对齐为 ≤4500 元起 6 档 |

### 4. P3 推进：快速推荐进度提示

- smart.html 新增 `<p class="smart-hint" id="smartHint" role="status" aria-live="polite">`
- recommend.js 新增 `updateHint()`，每次点选后实时刷新：
  - 未齐：`还差 预算、用途 没选（共 3 项必答）。第 4-6 题可以跳过，不选就按主流默认给。`
  - 已齐：`必答 3 项已选好，点「生成推荐」出方案。第 4-6 题可跳过。`（`.ready` 类变主题色加粗）
- CSS 加 `.smart-hint` / `.smart-hint.ready`
- `aria-live="polite"` 让读屏用户也能收到进度变化

## 四、文件变更

8 个文件：`accessories.html`、`guide.html`、`index.html`、`looks.html`、`smart.html`、`css/styles.css`、`js/data.js`、`js/models.js`、`js/recommend.js`

## 五、验证

- `node --check` 三个 JS 全过
- 数据加载：plans 7 / partCatalog 16 / quiz 10 / updated 2026-09（核验 2026-09-13）
- emoji 219 → 99；破折号 18 → 6；2026-08 残留 0；45 款残留 0
- 剩余 emoji 逐行核对：全部落在图标位、星级、语义标记（✕）上

## 六、待办

- **P0** 2026-Q4 行情再核验
- **P3 剩余候选**：测评页「全选/全不选」快捷按钮、测评答完导出方案（复用已有 `__copyText`）

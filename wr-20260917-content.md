# wr-20260917-content

## 背景

承接 wr-20260917-seo（robots / sitemap / FAQPage / 站内搜索 / 404 / 打印样式），本轮转向**内容维度**——不加新功能，只修两类问题：

1. **同一事实在不同位置说法不一**（数字口径崩坏）
2. **该具体却泛化 / 该能点却点不动**（内容具体度与死链）

清单来源：全站内容盘点（7 个主页面全文 + `data.js` 全文 + `models.js` 统计 + `recommend.js` 关键函数定位）。

---

## 一、数字口径矛盾（严重 · 3 项）

| # | 位置 | 原文 | 改为 | 实际数据依据 |
|---|---|---|---|---|
| 1 | `index.html:68` | `45+` 真实型号 | `75` | 同页 129 行已写「75 款」；`models.js` 实测 75 款 |
| 2 | `plans.html:54` | 「**四套**整机搭配方案：经济实用 / 主流游戏 / 创作旗舰 / 便携移动」 | 「**六套**整机方案，覆盖 ¥3000–¥20000 各预算档，外加一套便携外设补强包」 | `data.js.plans` = 6 整机（t3000/t4500/t6000/t8500/t12000/t20000）+ 1 便携包 = 7 套 |
| 3 | `accessories.html:54 / :111` + meta ×3 | 「**8 类**共 75 款」/「按 **8 大**配件品类」 | 「**10 类**共 75 款」/「按 **10 大**配件品类」 | `data.js.categories` 实为 10 类（含 CPU / 显卡） |

**第 2、3 项的内部证据**：`plans.html:61`（人群区）写的是「六套」，`recommend.js` 站内搜索空结果提示写的是「本页收录 10 类配件」——**站内已经有一处说对了**，说明是复制粘贴残留而非设计意图。

三处 meta（`description` / `og:description` / `twitter:description`）同步改为精确口径（`plans` 由「多套」→「六套 + 便携补强包」；`accessories` 由列举 8 项 → 「10 类共 75 款」）。

---

## 二、内容具体度与死链（5 项）

### 1. FAQ 四张资源卡全是死链
`faq.html:240/244/248/252` 原为 `<a class="card resource-card reveal" href="#">`——点击页面纹丝不动。

**改法**：外层 `<a href="#">` → `<div class="card resource-card reveal">`，卡内工具名改为**真实外链**（`target="_blank" rel="noopener noreferrer"`，与站内 `productImageLink` 既有做法一致）：

| 卡片 | 落地链接 |
|---|---|
| 检测工具 | cpuid.com（CPU-Z）/ hwinfo.com / crystalmark.info（CrystalDiskInfo）/ aida64.com |
| 跑分与评测 | benchmarks.ul.com（3DMark）/ maxon.net（Cinebench）/ geekbench.com |
| 社区与答疑 | chiphell.com / smzdm.com / bilibili.com / zhihu.com |
| 选购比价 | manmanbuy.com（慢慢买历史价）/ smzdm.com / jd.com / taobao.com |

**为什么改结构而不是给单链**：一张卡里列了 3-4 个工具，若外层只挂其中一个站的链接，用户会误以为整卡指向该站。改 `div` 后每个工具名单独可点，语义正确。CSS 侧确认 `.resource-card` 仅依赖 `display:block`，改 `div` 不破坏布局；新增 `.res-link`（`color: primary` + hover 下划线），因为全局 `a { color: inherit; text-decoration: none }` 会让卡内链接看不出来。

### 2. 泛化表述点名（`faq.html:250`）
原「硬件论坛、问答平台与视频教程渠道」→ 点名 Chiphell / 什么值得买 / B 站硬件区 / 知乎。

动机：同站 `guide.html` 的软件推荐是**点名道姓**的（Everything / Snipaste / 7-Zip / Dism++），一边具体一边抽象，读者能明显感到「虚」。

### 3. 文案重复错字（`guide.html:222`）
「功率留 30% 余量，**认 认** 80 PLUS **认证 认证**。」→ 「认准 80 PLUS 认证。」

### 4. 参数速查表覆盖率不足（`guide.html:299-304`）
原表仅 5 行（CPU / GPU / 内存 / 存储 / 显示器），但同页正文涉及 8 个部件。**补 5 行**：主板 / 电源 / 散热 / 机箱 / 网络，现 10 行，并与正文口径对齐（电源「留 30% 余量」、网络「千兆 + Wi-Fi 6」均取自正文既有说法，未新造数据）。

### 5. 正文全靠 JS 注入（`looks.html` / `plans.html` / `smart.html`）
三页主体内容由 `recommend.js` 运行时写入，HTML 本体为空。**各加一段 `<noscript>` 摘要**（均在 `<main>` 内，结构合法）：说明需要 JS、列出该页核心信息（6 种颜值风格名 / 六档方案预算 / 测评用途），并给出无 JS 时的替代路径。

---

## 三、一致性与细节（5 项）

| # | 问题 | 处理 |
|---|---|---|
| 1 | 6 个页面 `<h1 class="section-title">…</h2>` 标签闭合错配 | 6 处全部改 `</h1>` |
| 2 | 首页 3 张 build-card 内链全指 `guide.html#scenarios` | 分别指向 `plans.html#plan-t3000 / #plan-t6000 / #plan-t12000`，文案改「查看该方案配置单 →」 |
| 3 | 方案卡无锚点，无法深链 | `recommend.js:403` 给 `.plan-card` 加 `id="plan-{id}"` |
| 4 | 页脚 8 页同句且措辞失实 | 「知识框架版」→「装机知识库」（站点已是完整知识库，非框架版） |
| 5 | sticky 头部遮挡锚点 | CSS 加 `.section[id], .plan-card[id] { scroll-margin-top: 90px; }`——顺带修好既有的板块锚点遮挡 |

---

## 四、验证

| 项 | 结果 |
|---|---|
| JS 语法 `node --check` | 4 个文件全过 |
| HTML 标签配对（11 个 HTML，栈式校验） | 11/11 OK |
| 本轮 29 项改动逐条核对 | 29/29 PASS |
| 残留扫描（`45+` / `四套` / `8 类` / `8 大` / `知识框架版` / `href="#"` / `认 认` / `<h1>…</h2>` / `guide.html#scenarios`） | 全部 0 命中 |
| 数据口径 | models 75 / categories 10 / plans 7，与页面文案一致 |
| 速查表 | 10 行（CPU·GPU·内存·存储·主板·电源·散热·机箱·显示器·网络） |
| FAQ 资源卡 | 死链 0 / 有效卡 4 |
| 本地预览 | 8 页 + CSS + JS 全部 200 |

---

## 五、过程中踩到的工具坑（重要，需固化）

**同一批并行 Edit 中，对同一文件的多个编辑会竞态覆盖，导致部分写入静默丢失**（Edit 仍返回「成功」）。

本轮实际发生 3 次：
- `guide.html` 速查表补 5 行 —— 被同批的 h1 修正覆盖，丢失
- `index.html` 办公卡链接 t3000 —— 被同批第 2、3 张卡编辑覆盖，丢失
- `plans.html:67`「六套」→「六套整机方案」 —— 被同批 meta 编辑覆盖，丢失

**教训**：**对同一文件的多个编辑必须串行发送（一次消息一个 Edit），或合并成一次 Edit**；不同文件之间并行是安全的。

**验证也必须以「回读文件」为准**——本轮正是靠「改动逐条核对脚本」才发现了 3 处静默丢失，若只看 Edit 的返回信息会误判为全部完成。

---

## 六、剩余事项（未做，供后续排期）

| 优先级 | 事项 | 说明 |
|---|---|---|
| P2 | 7 套方案差异化 | 每套 `parts` 最后三条（显示器 / 键鼠）几乎重复，真正分层的只有 CPU / 显卡 / 内存 3 类，削弱「加钱加了什么」的说服力。改造需同时核对行情价，建议单独立项 |
| P3 | `models.js` 的 `valueNote` 扩写 | 现状正文约 18-35 字（75/75 全有），若扩到 40-60 字并补「对比谁 / 不适合谁」，推荐可靠感会提升。**风险**：75 条批量扩写需真实产品知识，易产生编造，建议按品类分批人工校对 |
| P3 | 断点统一 | 现有 760/880/480/560/640 五个值（沿用 wr-20260917-seo 的判断：轻微项） |

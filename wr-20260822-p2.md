# 工作记录 wr-20260822-p2（#7 真·自动同步脚手架 + 密钥配置说明）

版本：pc-guide-site / 提交待定
关联：2026-08-22 评估清单 #7（P2 真·自动同步）

## 一、本次交付
新增 `functions/price.js`（Cloudflare Pages Function）：
- 路由 `/price?q=关键词`，返回比价 JSON。
- **缺密钥不崩站**：未配置 `PRICE_API_URL` / `PRICE_API_KEY` 时返回明确提示 JSON，前端可继续用「跳转淘宝/京东查价」降级。
- 可选 KV 缓存（绑定 `PRICE_CACHE` 命名空间后启用，1 小时 TTL）。
- 当前前端仍走官方跳转查价，本 Function 为**可选增强**，部署后不影响现有功能。

## 二、密钥从哪来（比价 API 供应商，三选一）
| 方案 | 申请入口 | 拿到什么 | 备注 |
|---|---|---|---|
| 京东联盟（推荐） | jingfen.jd.com 申请成为推客 | `app_key` / `app_secret` | 用 app_secret 换 `access_token`，搜索商品拿实时价 |
| 淘宝客 | pub.alimama.com | `appkey` / `secret` | 类似京东，需备案/审核 |
| 第三方比价 API | 自建或聚合服务 | 一个 `API Key` | 成本与稳定性需自担 |

> 对「小白电脑选购站」而言，**当前「跳转淘宝/京东查官方实时价」已足够**，P2 是体验增强而非必需。是否值得上，取决于你愿不愿意承担 API 申请、合规与维护成本。

## 三、密钥怎么配（Cloudflare Pages 后台，3 步）
1. 登录 Cloudflare Dashboard → **Workers & Pages** → 项目 **pc-guide** → **Settings** → **Environment variables**。
2. 在 **Production** 下新增两个变量（值填你申请到的上游地址与 key）：
   - `PRICE_API_URL` = `https://你的比价上游地址?keyword=`（Function 会拼接关键词）
   - `PRICE_API_KEY` = `你的密钥`（类型选 Secrets 更稳妥）
   - （可选）**KV** 标签页绑定一个命名空间，变量名填 `PRICE_CACHE`。
3. 保存后回到 **Deployments** 点 **Redeploy**（或重新推送一次），新变量即生效。

## 四、验证
- 部署后访问 `https://你的域名/price?q=RTX%205060`：
  - 未配密钥 → 返回 `{"ok":false,"error":"PRICE_API_URL / PRICE_API_KEY 未配置",...}`（预期，站点照常）。
  - 已配密钥 → 返回 `{"ok":true,"data":[...]}`（具体字段依上游 API 而定，前端需按返回结构再接渲染）。

## 五、结论
- #7 以「可部署脚手架 + 缺密钥优雅降级」方式交付，**不破坏既有务实静态架构**。
- 密钥由你侧申请并填入 Cloudflare 环境变量，AI 无法代申请（涉及账号与合规）。
- P2 维持「可选增强」定位；若无强需求，保持现有跳转查价即可，零成本零风险。

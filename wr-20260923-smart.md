# wr-20260923-smart — 智能推荐「最高档配置太低端」修复

**触发**：用户反馈「智能推荐选择最高档看到的配置太过低端，需要补充更多高端的配件」

**版本**：待提交（前序 `c2b213a`）

---

## 一、诊断：用真实数据复现问题

写脚本（Node + `vm` 加载 `data.js` / `models.js`）复刻 `matchPlan` / `recommendItems` 逻辑，遍历 6 个预算档位：

### 根因 1 —— 预算档位与方案价格系统性脱节（主因）

`matchPlan()` 用**档位标识数字**去匹配**方案实际价格区间**（`b >= price[0]*0.85 && b <= price[1]*1.15`）。2026-09 行情上行后 `plans.price` 已整体上移，但档位数字没跟着走：

| 用户选的档位 | 实际拿到的方案 | 方案真实价 | 偏差 |
|---|---|---|---|
| 7000-10500 元 | 核显入门档 t3000 | ¥4200-5500 | 偏低一档 |
| 10500-14500 元 | 独显入门档 t4500 | ¥6500-8500 | 偏低一档 |
| 14500-22000 元 | 主流游戏档 t6000 | ¥10500-13000 | 偏低一档 |
| **22000 元+** | **高帧游戏档 t8500** | **¥14500-18500** | **偏低两档** |

**t12000（¥22000-28000）与 t20000（¥32000-42000）在任何档位下都无法被选中**——t20000 需要 `b >= 32000×0.85 = 27200`，而最高档传入的 `b` 只有 20000。

### 根因 2 —— 配件推荐没有预算感知（次因）

`recommendItems()` 的排序是**纯性价比等级优先**：

```js
var d = (gradeOrder[b.valueGrade] || 0) - (gradeOrder[a.valueGrade] || 0);
return d !== 0 ? d : b.rating - a.rating;
```

`cap` 只是上限，不参与排序。而 **S 级「性价比王」通常正是中端价位**，于是预算越高也照样选 S 级中端件。实测旗舰档（t20000，cap=45360）的推荐：

| 部件 | 推荐 | 同方案配置单写的 |
|---|---|---|
| CPU | Ryzen 5 8500G ¥900-1350 | Ultra 9 285K ¥4299-5300 |
| 显卡 | RTX 5060 8G ¥2300-2600 | RTX 5080 ¥11000-15000 |
| 散热 | 利民 AX120 R SE ¥60-89 | 360 水冷 ¥389-720 |

**同一页面自我打脸，差 5 倍。**

### 根因 3 —— 型号库高端段位缺口 + 显卡价格滞后

| 部件 | 库内最高价 | 配置单需求 | 判定 |
|---|---|---|---|
| 显示器 | ¥2499 | 4K MiniLED ¥3500-5000 | 缺口 |
| 键鼠 | ¥304 | 客制化键盘+旗舰鼠标 ¥1500-2500 | 缺口 |

显卡价格经联网核对（显卡日报 2026-09-17 / videocardprices.com 2026-09-20），**库内普遍低于行情 20-50%**：

| 型号 | 库内原价 | 2026-09 行情 |
|---|---|---|
| RTX 5080 16G | ¥8200-10300 | ¥12399-19999 |
| RTX 5070 Ti 16G | ¥7500-8500 | ¥8700-11000 |
| RTX 5070 12G | ¥4300-5400 | ¥6799 |
| RTX 5060 Ti 16G | ¥3300-3800 | ≈¥5500 |

> 顺带澄清了此前记为 P1 的「plans 与 models.js 价格不一致」：**plans 里的数字更接近真实行情，是 models.js 滞后**。

---

## 二、修复内容

### A. 算法层（`js/recommend.js`）

1. **档位重标定**：`budgetOpts` 增加 `budget` 字段（真实预算中值），label 与 `plans.price` 实际区间对齐；6 档与 6 套整机一一对应：
   `5500 元以内→t3000 / 5500-8500→t4500 / 8500-13000→t6000 / 13000-18500→t8500 / 18500-30000→t12000 / 30000 元以上→t20000`
2. **`matchPlan()`** 改用 `opt.budget` 真实预算值（不再 `parseInt(档位 id)`）
3. **`budgetOrder`**（深度测评用）同步重标定：`{b3000:4800, b4500:7500, b6000:11000, b8500:16000, b12000:24000, b20000:37000}`
4. **新增 `planPartTargets(plan)`**（提到 IIFE 顶层，快速推荐与深度测评共用）：从方案**自己的配置单**反读各类部件的目标价位（价格中值）。方案说配 RTX 5080，配件推荐就不该给 RTX 5060。
5. **`recommendItems()` 改造**：
   - 目标价位 = 配置单同类部件价位；配置单未列的类别按整机中值 3% 兜底
   - 只在目标价位 `[0.6x, 1.5x]` 窗口内挑选；窗口无货才退化为「最接近目标价位的 3 款」
   - 排序改为**贴近度优先**（等级只在价差相同时作平手加分），否则 S 级中端件会一路压过高端的 A 级件
   - 核显机（配置单无「显卡」行）**不再推荐独显**
6. **新增 `PART_SPLIT`**：配置单里「键鼠」写的是键盘+鼠标组合价，折算到单件按 75%
7. **`pickPlan()`**（深度测评）同分时取**价位更高**的方案（原逻辑取靠前的＝便宜的，等于预算够也停在低档）
8. **`pickItems()`** 的 `allowance` 改用配置单目标价位；预算匹配权重由 `+2` 改为连续贴近度 `2 + 5×ratio`（须高于性价比等级，否则同样被压回中端）
9. **`initBudgetTool`** 预设档 `[3000…20000]` → `[5000, 8000, 12000, 17000, 25000, 37000]`（原最高点触不到旗舰档）

### B. 数据层（`js/models.js`，联网核价后新增/校正）

**新增 11 款（75 → 86 款）**

| 类别 | 新增 |
|---|---|
| 显卡 | RTX 5090D 32G ¥39999-45000（真旗舰，C 级：溢价明显） |
| 显示器 | 雷鸟 27U8A ¥1889-2299 · 雷神 CU27F160M-EVO ¥2199-2499 · HKC G27M7Pro ¥2399-2699 · AOC AG276UZD ¥3299-3699 · 微星 MAG321UPX 32"4K QD-OLED ¥4199-4599 |
| 键鼠 | ROG 夜魔 X ¥1849-1999 · Keychron Q13 Ultra 8K ¥1601-1899 · ROG 龙鳞 2 Ace ¥909-999 · 罗技 MX Master 4 ¥999-1099 · 万灵竞记 幻63 ¥1799 |

**价格校正 4 款**：RTX 5080 ¥8200-10300→¥12300-16000 · RTX 5070 Ti ¥7500-8500→¥8700-10500 · RTX 5070 ¥4300-5400→¥6300-7200 · RTX 5060 Ti 16G ¥3300-3800→¥5000-6000（评级 S→A，涨约 55% 后性价比不再突出）

> 价格来源：显卡日报 2026-09-17、videocardprices.com 2026-09-20、京东/天猫在售页、ZOL/太平洋评测。均为**参考行情**，页面已统一标注「以电商实时价为准」。

### C. 口径同步

型号总数 75→86，同步 `index.html`（stat-num + 卡片文案）、`accessories.html`（meta×3 + section-desc）、`404.html`；`data.js` 的 `quiz.budget` label 与 `budgetOpts` 同口径。

---

## 三、验证

Node + `vm` 加载真实数据复刻新逻辑，6 组共 12 项断言 **全 PASS**：

- 6 档位 → 方案**单调一一对应**，t12000 / t20000 可达
- 显卡档次随预算单调上移：— / ¥3100 / ¥5500 / ¥6750 / ¥9600 / **¥14150**
- 最高档明细全部对齐配置单（Ultra 9 285K + RTX 5080 + 4K QD-OLED + Keychron）
- 低预算档回归正常（核显机不推独显、CPU ≤¥1600）
- 深度测评最高档可达 t20000

其他：4 个 JS `node --check` 全过；本地预览 12 资源全 200；16 项改动落地检查全 PASS；「75 款」残留扫描零命中。

---

## 四、后续跟进

| 优先级 | 事项 |
|---|---|
| P2 | 其余品类价格核验：显示器/键鼠/音频的库内价格尚未逐一对照行情 |
| P2 | 机箱型号库仍仅 3 款（ATX 仅 2 款），高端全塔缺口未补 |
| P3 | `valueGrade` 评级体系未随涨价重估——本次只动了 RTX 5060 Ti 16G 一例 |
| 定期 | 下次行情核验 2026-11 底 ~ 12 月初（DRAM/GPU 仍在涨价周期） |
